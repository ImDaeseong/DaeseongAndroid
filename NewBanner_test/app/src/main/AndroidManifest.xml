<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Banner1styleActivity" />
        <activity android:name=".Banner2styleActivity" />
        <activity android:name=".Banner3styleActivity" />
        <activity android:name=".Banner4styleActivity" />
        <activity android:name=".Banner5styleActivity" />
        <activity android:name=".Banner6styleActivity" />
        <activity android:name=".Banner7styleActivity" />
    </application>

</manifest>